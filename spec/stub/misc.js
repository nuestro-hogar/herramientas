/**
 * Stub class for Misc class
 *
 * Provides information about Gnome Shell.
 *
 * @type {{getCurrentExtension: extensionUtils.getCurrentExtension}}
 */
var extensionUtils = {
    getCurrentExtension: function() {
        return {
            imports: imports
        };
    }
};

var util = {};
